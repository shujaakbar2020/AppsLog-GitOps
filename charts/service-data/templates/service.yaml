apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-data.name" . }}-{{ .Release.Name }}
  labels:
    {{- include "service-data.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "service-data.selectorLabels" . | nindent 4 }}